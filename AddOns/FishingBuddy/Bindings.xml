<Bindings>
	<!-- window commands -->
	<Binding name="FISHINGBUDDY_TOGGLE" header="FISHINGBUDDY_BINDINGS" category="ADDONS">
		if ( keystate == "down" ) then
			FishingBuddy.Command("");
		end
	</Binding>
	<Binding name="TOGGLEFISHINGBUDDY_LOC" category="ADDONS">
		if ( keystate == "down" ) then
			ToggleFishingBuddyFrame("FishingLocationsFrame");
		end
	</Binding>
	<Binding name="TOGGLEFISHINGBUDDY_OPT" category="ADDONS">
		if ( keystate == "down" ) then
			ToggleFishingBuddyFrame("FishingOptionsFrame");
		end
	</Binding>
	<!-- commands -->
	<Binding name="FISHINGBUDDY_SWITCH" category="ADDONS">
		if ( keystate == "down" ) then
			FishingBuddy.Command(FBConstants.SWITCH);
		end
	</Binding>
	<Binding name="FISHINGBUDDY_GOFISHING" category="ADDONS" />
</Bindings>